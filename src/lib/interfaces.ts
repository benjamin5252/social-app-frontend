export interface UserObj {
  name: string,
  userName: string,
  id: number,
  profilePic: string
}

export interface msgObj {
  reply: string,
  message: string,
  from: number,
  to: number,
  sendAt: number
}